import React from 'react';

const NoticeBoard = () => {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'column',
        minHeight: '100vh',
        margin: '0',
      }}
    >
      <h1>NoticeBoard</h1>
    </div>
  );
};

export default NoticeBoard;
